#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    //cin.ignore(); //if we use getline next.... this line will clean the buffer
    //if they want a blank line, uncomment next line
    // cin.ignore();
    cin.ignore(); // for getline()
    // while(scanf("%d %d",&match,&goal)==2)
    // while(scanf("%lld %lld %lld",&a,&b,&c))
    //while(cin>>t);
    while (t--)
    {
        string s;
        while (getline(cin, s))
        {
            if (s.empty())
                break;
            //process
        }

        //output

        if (t > 0)
            cout << endl;
    }
    return 0;
}

