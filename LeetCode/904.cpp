#include<bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int totalFruit(vector<int>& fruits)
    {
        int i=0, j=0;
        deque<int>cur;
        cur.push_back(fruits[i]);
        while(i<fruits.size() and j<fruits.size())
        {
            while(cur)
        }
    }
};
int main()
{
    Solution obj;
    vector<int>vec = {0,1,2,2};
    cout<<obj.totalFruit(vec);
}
